#include <stdio.h>

int main() {
    int i = 0;
    // This loop will iterate at least once and then while i < 5
    do {
        printf("i = %d\n", i);
        i++;
    } while (i < 5);
    return 0;
}